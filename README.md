# homelab-automation

Infrastructure automation for the homelab using Terraform and Ansible.

## Structure

- `terraform/proxmox`: VM provisioning on Proxmox
- `ansible`: host configuration and cluster bootstrap
- `docs`: decisions, runbooks, and architecture notes