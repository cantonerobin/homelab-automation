# Copy this file to secrets.yml and fill in the values.
# secrets.yml is in .gitignore — never commit!
#
# Proton Pass: back up all values there as Secure Notes.

# TrueNAS API Key (TrueNAS UI → Credentials → API Keys)
truenas_api_key: ""

# TrueNAS VM VNC password
truenas_vm_vnc_password: ""

# Gotify app token (Gotify UI → Apps → Token)
disk_monitor_gotify_token: ""

# Hetzner offsite backup — rclone crypt encryption
# ⚠️ Without these passwords the backups on the Storage Box are unreadable!
# Back up in Proton Pass: Secure Note "hetzner-rclone-crypt"
hetzner_rclone_encryption_password: ""
hetzner_rclone_encryption_salt: ""
