---
- name: Ensure prerequisites on control node
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Install sshpass (for password auth during bootstrap)
      apt:
        name: sshpass
        state: present
      become: true

- name: VM base configuration
  hosts: "{{ target | default('all') }}"
  become: true
  roles:
    - vm_base
